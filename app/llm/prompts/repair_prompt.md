Your interpretation of the target note failed validation:

{errors}

Re-read the target note and the battery context, and return a corrected interpretation as a full
new JSON object following the same schema. Do not just patch one field — recompute hours, factor,
minimum_energy_kwh and max_grid_kwh consistently with your corrected reading. Rules reminder:
{rule_reminder}
