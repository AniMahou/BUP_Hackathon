services:
  gridwise:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - LLM_MODEL=${LLM_MODEL:-gemini-flash-lite-latest}
      - LLM_FALLBACK_MODELS=${LLM_FALLBACK_MODELS:-gemini-flash-latest}
      - PORT=8000
      - WEB_CONCURRENCY=2
